import { a as m$1, c as se$1, d as e, f as n, g as n$1, h as i, i as f$1, n as Y$1, r as _$1, s as p$1, t as T$1, v as t } from "./png-encoder-B7pgEY_K.js";
//#region node_modules/etiket/dist/_chunks/micro.mjs
function f(e, t, n, r, i = 1) {
	let a = n * i, o = (n - a) / 2;
	switch (r) {
		case `square`: return p(e + o, t + o, a);
		case `rounded`: return m(e + o, t + o, a, a * .25);
		case `dots`: return h(e + n / 2, t + n / 2, a / 2);
		case `diamond`: return g(e + n / 2, t + n / 2, a);
		case `classy`: return _(e + o, t + o, a);
		case `classy-rounded`: return v(e + o, t + o, a);
		case `extra-rounded`: return m(e + o, t + o, a, a * .5);
		case `vertical-line`: return y(e + o, t, n, a);
		case `horizontal-line`: return b(e, t + o, n, a);
		case `small-square`: return p(e + n * .15, t + n * .15, n * .7);
		case `tiny-square`: return p(e + n * .25, t + n * .25, n * .5);
		default: return p(e + o, t + o, a);
	}
}
function p(e, t, n) {
	return `M${e},${t}h${n}v${n}h-${n}z`;
}
function m(e, t, n, r) {
	return r = Math.min(r, n / 2), `M${e + r},${t}h${n - 2 * r}a${r},${r},0,0,1,${r},${r}v${n - 2 * r}a${r},${r},0,0,1,-${r},${r}h-${n - 2 * r}a${r},${r},0,0,1,-${r},-${r}v-${n - 2 * r}a${r},${r},0,0,1,${r},-${r}z`;
}
function h(e, t, n) {
	return `M${e - n},${t}a${n},${n},0,1,0,${2 * n},0a${n},${n},0,1,0,-${2 * n},0z`;
}
function g(e, t, n) {
	let r = n / 2;
	return `M${e},${t - r}l${r},${r}l-${r},${r}l-${r},-${r}z`;
}
function _(e, t, n) {
	let r = n * .35;
	return `M${e},${t}h${n - r}a${r},${r},0,0,1,${r},${r}v${n - r}h-${n}v-${n}z`;
}
function v(e, t, n) {
	let r = n * .35;
	return `M${e},${t}h${n - r}a${r},${r},0,0,1,${r},${r}v${n - 2 * r}a${r},${r},0,0,1,-${r},${r}h-${n - r}v-${n}z`;
}
function y(e, t, n, r) {
	return `M${e},${t}h${r}v${n}h-${r}z`;
}
function b(e, t, n, r) {
	return `M${e},${t}h${n}v${r}h-${n}z`;
}
function x(e, t, n, r = `square`) {
	let i = n * 7;
	switch (r) {
		case `rounded`: {
			let r = n * 1.5;
			return C(e, t, i, i, r) + w(e + n, t + n, i - 2 * n, i - 2 * n, r * .5);
		}
		case `dots`: {
			let r = i / 2, a = r - n;
			return h(e + r, t + r, r) + h(e + r, t + r, a);
		}
		case `extra-rounded`: {
			let r = n * 2.5;
			return C(e, t, i, i, r) + w(e + n, t + n, i - 2 * n, i - 2 * n, r * .6);
		}
		case `classy`: {
			let r = n * 1.5;
			return C(e, t, i, i, r) + w(e + n, t + n, i - 2 * n, i - 2 * n, r * .5);
		}
		default: return `M${e},${t}h${i}v${i}h-${i}zM${e + n},${t + n}v${i - 2 * n}h${i - 2 * n}v-${i - 2 * n}z`;
	}
}
function S(e, t, n, r = `square`) {
	let i = n * 3, a = e + n * 3.5, o = t + n * 3.5;
	switch (r) {
		case `dots`: return h(a, o, i / 2);
		case `rounded`: return m(e + n * 2, t + n * 2, i, n * .75);
		default: return p(e + n * 2, t + n * 2, i);
	}
}
function C(e, t, n, r, i) {
	return `M${e + i},${t}h${n - 2 * i}a${i},${i},0,0,1,${i},${i}v${r - 2 * i}a${i},${i},0,0,1,-${i},${i}h-${n - 2 * i}a${i},${i},0,0,1,-${i},-${i}v-${r - 2 * i}a${i},${i},0,0,1,${i},-${i}z`;
}
function w(e, t, n, r, i) {
	return C(e, t, n, r, i);
}
var T = 0;
function E() {
	return `etiket-grad-${T++}`;
}
function D() {
	T = 0;
}
function O(e) {
	return typeof e == `object` && !!e && `type` in e;
}
function k(e) {
	let t = E();
	return e.type === `linear` ? {
		id: t,
		svg: A(t, e)
	} : {
		id: t,
		svg: j(t, e)
	};
}
function A(e, t) {
	let r = (t.rotation ?? 0) * Math.PI / 180;
	return `<linearGradient id="${e}" x1="${Math.round((50 - Math.sin(r) * 50) * 100) / 100}%" y1="${Math.round((50 + Math.cos(r) * 50) * 100) / 100}%" x2="${Math.round((50 + Math.sin(r) * 50) * 100) / 100}%" y2="${Math.round((50 - Math.cos(r) * 50) * 100) / 100}%">${t.stops.map((e) => `<stop offset="${e.offset * 100}%" stop-color="${i(e.color)}"/>`).join(``)}</linearGradient>`;
}
function j(e, t) {
	return `<radialGradient id="${e}" cx="50%" cy="50%" r="50%">${t.stops.map((e) => `<stop offset="${e.offset * 100}%" stop-color="${i(e.color)}"/>`).join(``)}</radialGradient>`;
}
function M(e) {
	let t = new DataView(e.buffer, e.byteOffset, e.byteLength), n = t.getUint16(0, !0), r = t.getUint16(2, !0), i = t.getUint16(4, !0);
	if (n !== 0 || r !== 1 && r !== 2 || i === 0) throw Error(`Invalid ICO file`);
	let a;
	for (let e = 0; e < i; e++) {
		let n = 6 + e * 16, r = t.getUint8(n) || 256, i = t.getUint8(n + 1) || 256, o = t.getUint32(n + 8, !0), s = t.getUint32(n + 12, !0), c = r * i;
		(!a || c > a.width * a.height) && (a = {
			width: r,
			height: i,
			size: o,
			offset: s
		});
	}
	let o = a, s = e.subarray(o.offset, o.offset + o.size);
	return s.length >= 4 && t.getUint32(o.offset) === 2303741511 ? `data:image/png;base64,${B(s)}` : N(s, o.width, o.height);
}
function N(e, t, n) {
	let r = new DataView(e.buffer, e.byteOffset, e.byteLength), i = r.getUint32(0, !0), a = r.getInt32(4, !0) || t, o = n, s = r.getUint16(14, !0), l = r.getUint32(16, !0);
	if (l !== 0 && l !== 3) throw Error(`Unsupported BMP compression: ${l}`);
	let u = new Uint8Array(a * o * 4);
	if (s === 32) P(e, i, a, o, u);
	else if (s === 24) F(e, i, a, o, u);
	else if (s <= 8) I(e, i, a, o, s, u);
	else throw Error(`Unsupported BMP bit depth: ${s}`);
	if (s < 32) {
		let t = R(a, o, s, e, i), n = i + L(e, i, s) + t;
		n < e.length && z(e, n, a, o, u);
	}
	return `data:image/png;base64,${B(se$1(a, o, u))}`;
}
function P(e, t, n, r, i) {
	let a = t, o = n * 4;
	for (let t = 0; t < r; t++) {
		let s = a + (r - 1 - t) * o, c = t * n * 4;
		for (let t = 0; t < n; t++) {
			let n = s + t * 4, r = c + t * 4;
			i[r] = e[n + 2], i[r + 1] = e[n + 1], i[r + 2] = e[n], i[r + 3] = e[n + 3];
		}
	}
}
function F(e, t, n, r, i) {
	let a = t + L(e, t, 24), o = Math.ceil(n * 3 / 4) * 4;
	for (let t = 0; t < r; t++) {
		let s = a + (r - 1 - t) * o, c = t * n * 4;
		for (let t = 0; t < n; t++) {
			let n = s + t * 3, r = c + t * 4;
			i[r] = e[n + 2], i[r + 1] = e[n + 1], i[r + 2] = e[n], i[r + 3] = 255;
		}
	}
}
function I(e, t, n, r, i, a) {
	let o = L(e, t, i), s = e.subarray(t, t + o), c = t + o, l = Math.ceil(n * i / 32) * 4;
	for (let t = 0; t < r; t++) {
		let o = c + (r - 1 - t) * l, u = t * n * 4;
		for (let t = 0; t < n; t++) {
			let n;
			if (i === 8) n = e[o + t];
			else if (i === 4) {
				let r = e[o + (t >> 1)];
				n = t & 1 ? r & 15 : r >> 4 & 15;
			} else n = e[o + (t >> 3)] >> 7 - (t & 7) & 1;
			let r = n * 4, c = u + t * 4;
			a[c] = s[r + 2], a[c + 1] = s[r + 1], a[c + 2] = s[r], a[c + 3] = 255;
		}
	}
}
function L(e, t, n) {
	if (n >= 16) return 0;
	let r = new DataView(e.buffer, e.byteOffset, e.byteLength);
	return ((t >= 36 ? r.getUint32(32, !0) : 0) || 1 << n) * 4;
}
function R(e, t, n, r, i) {
	return Math.ceil(e * n / 32) * 4 * t;
}
function z(e, t, n, r, i) {
	let a = Math.ceil(n / 32) * 4;
	for (let o = 0; o < r; o++) {
		let s = t + (r - 1 - o) * a, c = o * n * 4;
		for (let t = 0; t < n; t++) {
			let n = s + (t >> 3);
			if (n >= e.length) return;
			(e[n] >> 7 - (t & 7) & 1) == 1 && (i[c + t * 4 + 3] = 0);
		}
	}
}
function B(e) {
	let t = ``;
	for (let n = 0; n < e.length; n++) t += String.fromCharCode(e[n]);
	return btoa(t);
}
var ee = /<script[\s>]|javascript:|on[a-z]+\s*=|<foreignObject[\s>]/i;
function te(t$1, r, i$1, a) {
	let o = t$1.size ?? .3, s = t$1.margin ?? 0, c = t$1.hideBackgroundDots ?? !0, l = r * i$1, u = l * o, d = a * i$1 + (l - u) / 2, f = a * i$1 + (l - u) / 2, p = /* @__PURE__ */ new Set();
	if (c) {
		let e = Math.floor((r - r * o) / 2 - s / i$1), t = Math.ceil((r + r * o) / 2 + s / i$1);
		for (let n = Math.max(0, e); n < Math.min(r, t); n++) for (let i = Math.max(0, e); i < Math.min(r, t); i++) p.add(`${n},${i}`);
	}
	let m = ``;
	if (t$1.backgroundColor) {
		let e = s;
		m += `<rect x="${d - e}" y="${f - e}" width="${u + 2 * e}" height="${u + 2 * e}" fill="${i(t$1.backgroundColor)}" rx="4"/>`;
	}
	if (t$1.svg) {
		if (ee.test(t$1.svg)) throw new t(`logo.svg contains potentially dangerous content (script, event handlers, or foreignObject)`);
		m += `<svg x="${d}" y="${f}" width="${u}" height="${u}" viewBox="0 0 1 1">`, m += `<g transform="scale(1)">${t$1.svg}</g></svg>`;
	} else if (t$1.path) m += `<path d="${i(t$1.path)}" transform="translate(${d},${f}) scale(${u / 100})" fill="currentColor"/>`;
	else if (t$1.imageUrl) {
		if (!/^(https?:|data:image\/)/i.test(t$1.imageUrl)) throw new t(`imageUrl must use https:, http:, or data:image/ scheme`);
		let r = t$1.imageUrl.match(/^data:image\/(x-icon|vnd\.microsoft\.icon);base64,(.+)$/i), i$2 = t$1.imageUrl;
		if (r) {
			let e = atob(r[2]), t = new Uint8Array(e.length);
			for (let n = 0; n < e.length; n++) t[n] = e.charCodeAt(n);
			i$2 = M(t);
		}
		let a = t$1.imageWidth ?? u, o = t$1.imageHeight ?? u, s = d + (u - a) / 2, c = f + (u - o) / 2;
		m += `<image href="${i(i$2)}" x="${s}" y="${c}" width="${a}" height="${o}"/>`;
	}
	return {
		svg: m,
		hiddenModules: p
	};
}
function V(e) {
	return e.replace(/&/g, `&amp;`).replace(/</g, `&lt;`).replace(/>/g, `&gt;`).replace(/"/g, `&quot;`);
}
function H(e, t = {}) {
	let { size: r = 200, color: i$3 = `#000`, background: a = `#fff`, margin: o = 4, dotType: s = `square`, dotSize: c = 1, shape: l = `square`, corners: u, logo: d, xmlDeclaration: p = !1, unit: m, ariaLabel: h, role: g = `img`, title: _, desc: v } = t;
	D();
	let y = e.length, b = r / (y + o * 2), C = m ? `${r}${m}` : `${r}`, w = [], T = [];
	p && T.push(`<?xml version="1.0" encoding="UTF-8"?>`);
	let E = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${r} ${r}" width="${C}" height="${C}" role="${i(g)}"`;
	if (h && (E += ` aria-label="${i(h)}"`), E += `>`, T.push(E), _ && T.push(`<title>${V(_)}</title>`), v && T.push(`<desc>${V(v)}</desc>`), a !== `transparent`) if (O(a)) {
		let e = k(a);
		w.push(e.svg), T.push(`<rect width="100%" height="100%" fill="url(#${e.id})"/>`);
	} else T.push(`<rect width="100%" height="100%" fill="${i(a)}"/>`);
	let A = `#000`;
	if (O(i$3)) {
		let e = k(i$3);
		w.push(e.svg), A = `url(#${e.id})`;
	} else A = i(i$3);
	let j, M = ``;
	if (d) {
		let e = te(d, y, b, o);
		j = e.hiddenModules, M = e.svg;
	}
	let N = [
		{
			row: 0,
			col: 0,
			key: `topLeft`
		},
		{
			row: 0,
			col: y - 7,
			key: `topRight`
		},
		{
			row: y - 7,
			col: 0,
			key: `bottomLeft`
		}
	], P = /* @__PURE__ */ new Set();
	for (let e of N) for (let t = 0; t < 7; t++) for (let n = 0; n < 7; n++) P.add(`${e.row + t},${e.col + n}`);
	if (u) for (let e of N) {
		let t = u[e.key], r = (e.col + o) * b, i$4 = (e.row + o) * b, a = t?.outerShape ?? `square`, s = A;
		if (t?.outerColor) if (O(t.outerColor)) {
			let e = k(t.outerColor);
			w.push(e.svg), s = `url(#${e.id})`;
		} else s = i(t.outerColor);
		T.push(`<path d="${x(r, i$4, b, a)}" fill="${s}" fill-rule="evenodd"/>`);
		let c = t?.innerShape ?? `square`, l = A;
		if (t?.innerColor) if (O(t.innerColor)) {
			let e = k(t.innerColor);
			w.push(e.svg), l = `url(#${e.id})`;
		} else l = i(t.innerColor);
		T.push(`<path d="${S(r, i$4, b, c)}" fill="${l}"/>`);
	}
	else P.clear();
	let F = [];
	for (let t = 0; t < y; t++) for (let n = 0; n < y; n++) {
		if (!e[t][n] || j?.has(`${t},${n}`) || P.has(`${t},${n}`)) continue;
		let r = (n + o) * b, i = (t + o) * b;
		F.push(f(r, i, b, s, c));
	}
	if (F.length > 0 && T.push(`<path d="${F.join(``)}" fill="${A}"/>`), M && T.push(M), l === `circle`) {
		let e = r / 2, t = r / 2, n = r / 2;
		w.push(`<clipPath id="etiket-circle-clip"><circle cx="${e}" cy="${t}" r="${n}"/></clipPath>`);
		let i = p ? 2 : 1;
		_ && i++, v && i++;
		let o = a === `transparent` ? i : i + 1, s = T.splice(o);
		T.push(`<g clip-path="url(#etiket-circle-clip)">`), T.push(...s), T.push(`</g>`);
	}
	if (w.length > 0) {
		let e = p ? 2 : 1;
		T.splice(e, 0, `<defs>${w.join(``)}</defs>`);
	}
	return T.push(`</svg>`), T.join(``);
}
function U(e, t = {}) {
	let { compact: n = !0, margin: r = 2, invert: i = !1 } = t, a = e.length;
	if (n) return W(e, a, r, i);
	let o = t.dark ?? `██`, s = t.light ?? `  `, c = i ? s : o, l = i ? o : s, u = [];
	for (let e = 0; e < r; e++) u.push(l.repeat(a + r * 2));
	for (let t = 0; t < a; t++) {
		let n = l.repeat(r);
		for (let r = 0; r < a; r++) n += e[t][r] ? c : l;
		n += l.repeat(r), u.push(n);
	}
	for (let e = 0; e < r; e++) u.push(l.repeat(a + r * 2));
	return u.join(`
`);
}
function W(e, t, n, r) {
	let i = [];
	for (let a = -n; a < t + n; a += 2) {
		let o = ``;
		for (let i = -n; i < t + n; i++) {
			let n = G(e, a, i, t) !== r, s = G(e, a + 1, i, t) !== r;
			n && s ? o += `█` : n && !s ? o += `▀` : !n && s ? o += `▄` : o += ` `;
		}
		i.push(o);
	}
	return i.join(`
`);
}
function G(e, t, n, r) {
	return t < 0 || t >= r || n < 0 || n >= r ? !1 : !!e[t][n];
}
function K(e, t = {}) {
	let { size: n, margin: r, color: i, background: a, dotType: o, dotSize: s, shape: c, corners: u, logo: d, xmlDeclaration: f, ariaLabel: p, role: m, title: h, desc: g, ..._ } = t;
	return d && !_.ecLevel && (_.ecLevel = `H`), H(Y$1(e, _), {
		size: n,
		margin: r,
		color: i,
		background: a,
		dotType: o,
		dotSize: s,
		shape: c,
		corners: u,
		logo: d,
		xmlDeclaration: f,
		ariaLabel: p,
		role: m,
		title: h,
		desc: g
	});
}
function q(e, t) {
	return U(Y$1(e, t));
}
function J(e$1, t) {
	return e(K(e$1, t));
}
function Y(e, t) {
	return n(K(e, t));
}
var X = {
	1: { _: {
		numeric: 5,
		alphanumeric: 0,
		byte: 0,
		dataCW: 3,
		ecCW: 2
	} },
	2: {
		L: {
			numeric: 10,
			alphanumeric: 6,
			byte: 0,
			dataCW: 5,
			ecCW: 5
		},
		M: {
			numeric: 8,
			alphanumeric: 5,
			byte: 0,
			dataCW: 4,
			ecCW: 6
		}
	},
	3: {
		L: {
			numeric: 23,
			alphanumeric: 14,
			byte: 9,
			dataCW: 11,
			ecCW: 6
		},
		M: {
			numeric: 18,
			alphanumeric: 11,
			byte: 7,
			dataCW: 9,
			ecCW: 8
		}
	},
	4: {
		L: {
			numeric: 35,
			alphanumeric: 21,
			byte: 15,
			dataCW: 16,
			ecCW: 8
		},
		M: {
			numeric: 30,
			alphanumeric: 18,
			byte: 13,
			dataCW: 14,
			ecCW: 10
		},
		Q: {
			numeric: 21,
			alphanumeric: 12,
			byte: 9,
			dataCW: 10,
			ecCW: 14
		}
	}
}, Z = {
	1: { numeric: 3 },
	2: {
		numeric: 4,
		alphanumeric: 3
	},
	3: {
		numeric: 5,
		alphanumeric: 4,
		byte: 4
	},
	4: {
		numeric: 6,
		alphanumeric: 5,
		byte: 5
	}
}, ne = {
	1: { _: 0 },
	2: {
		L: 1,
		M: 2
	},
	3: {
		L: 3,
		M: 4
	},
	4: {
		L: 5,
		M: 6,
		Q: 7
	}
}, re = [
	17477,
	16754,
	20011,
	19228,
	21934,
	20633,
	24512,
	23287,
	26515,
	25252,
	28157,
	26826,
	30328,
	29519,
	31766,
	31009,
	1758,
	1001,
	3248,
	2439,
	5941,
	4610,
	7515,
	6252,
	9480,
	8255,
	12134,
	10833,
	13539,
	12756,
	16013,
	15290
], ie = [
	(e, t) => e % 2 == 0,
	(e, t) => (Math.floor(e / 2) + Math.floor(t / 3)) % 2 == 0,
	(e, t) => (e * t % 2 + e * t % 3) % 2 == 0,
	(e, t) => ((e + t) % 2 + e * t % 3) % 2 == 0
];
function ae(t$2, n = {}) {
	if (t$2.length === 0) throw new t(`Micro QR input must not be empty`);
	let r = /^\d+$/.test(t$2), i = !r && /^[0-9A-Z $%*+\-./:]+$/.test(t$2), a = r ? `numeric` : i ? `alphanumeric` : `byte`, { version: s, cap: c, ecKey: l } = oe(t$2, a, n), u = s * 2 + 9, f = se(t$2, new TextEncoder().encode(t$2), a, s, c), p = [];
	for (let e = 0; e < f.length; e += 8) {
		let t = 0;
		for (let n = 0; n < 8 && e + n < f.length; n++) t = t << 1 | f[e + n];
		p.push(t);
	}
	let m = c.ecCW > 0 ? T$1(p, c.ecCW) : [], h = [];
	if (s === 1 || s === 3) {
		for (let e = 0; e < p.length - 1; e++) _$1(h, p[e], 8);
		_$1(h, p[p.length - 1] >> 4, 4);
		for (let e of m) _$1(h, e, 8);
	} else for (let e of [...p, ...m]) _$1(h, e, 8);
	let g = Q(u);
	ce(g, u), le(g, h, u, s);
	let _ = ne[s][l], v = fe(g, u, n.mask);
	return $(g, v, u), pe(g, _, v), g.map((e) => e.map((e) => e === 1));
}
function oe(e, n, r) {
	let i = r.ecLevel;
	function a(e) {
		return e === 1 ? `_` : i && X[e][i] ? i : `L`;
	}
	if (r.version) {
		let e = r.version, n = a(e), i = X[e]?.[n];
		if (!i) throw new n$1(`Micro QR M${e} does not support EC level ${n}`);
		return {
			version: e,
			cap: i,
			ecKey: n
		};
	}
	let o = n === `byte` ? new TextEncoder().encode(e).length : e.length;
	for (let e = 1; e <= 4; e++) {
		let t = a(e), r = X[e][t];
		if (!r) continue;
		let i = n;
		if (typeof r[i] == `number` && o <= r[i]) return {
			version: e,
			cap: r,
			ecKey: t
		};
	}
	throw new n$1(`Data too long for Micro QR Code with ${n} mode`);
}
function se(e, t, n, r, i) {
	let o = [];
	r === 2 ? _$1(o, n === `numeric` ? 0 : 1, 1) : r === 3 ? _$1(o, n === `numeric` ? 0 : n === `alphanumeric` ? 1 : 2, 2) : r === 4 && _$1(o, n === `numeric` ? 0 : n === `alphanumeric` ? 1 : 2, 3);
	let c = Z[r][n];
	_$1(o, n === `byte` ? t.length : e.length, c), n === `numeric` ? o.push(...f$1(e)) : n === `alphanumeric` ? o.push(...p$1(e)) : o.push(...m$1(t));
	let l = i.dataCW * 8;
	if (_$1(o, 0, Math.min(r === 1 ? 3 : r === 2 ? 5 : r === 3 ? 7 : 9, l - o.length)), r === 1 || r === 3) for (; o.length < l;) o.push(0);
	else {
		for (; o.length % 8 != 0;) o.push(0);
		let e = !0;
		for (; o.length < l;) _$1(o, e ? 236 : 17, 8), e = !e;
	}
	return o;
}
function Q(e) {
	let t = Array.from({ length: e }, () => Array.from({ length: e }).fill(2));
	for (let e = 0; e < 8; e++) for (let n = 0; n < 8; n++) if (e === 7 || n === 7) t[e][n] = 0;
	else {
		let r = e === 0 || e === 6 || n === 0 || n === 6, i = e >= 2 && e <= 4 && n >= 2 && n <= 4;
		t[e][n] = r || i ? 1 : 0;
	}
	for (let n = 8; n < e; n++) t[0][n] = n % 2 == 0 ? 1 : 0, t[n][0] = n % 2 == 0 ? 1 : 0;
	return t;
}
function ce(e, t) {
	for (let n = 1; n <= 8; n++) n < t && (e[8][n] = 0, e[n][8] = 0);
}
function le(e, t, n, r) {
	let i = r === 1 || r === 3 ? 2 : 0, a = 0;
	for (let r = n - 1; r >= 1; r -= 2) for (let o = 0; o < n; o++) for (let s = 0; s < 2; s++) {
		let c = r - s;
		if (c < 0) continue;
		let l = r + i & 2 ? o : n - 1 - o;
		e[l][c] === 2 && (e[l][c] = a < t.length ? t[a] : 0, a++);
	}
}
function ue(e, t, n) {
	return !(t <= 7 && n <= 7 || t === 0 || n === 0 || t === 8 && n >= 1 && n <= 8 || n === 8 && t >= 1 && t <= 8);
}
function de(e, t) {
	let n = 0, r = 0;
	for (let i = 1; i < t; i++) n += e[i][t - 1], r += e[t - 1][i];
	return n <= r ? n * 16 + r : r * 16 + n;
}
function fe(e, t, n) {
	if (n !== void 0 && n >= 0 && n <= 3) return n;
	let r = 0, i = -1;
	for (let n = 0; n < 4; n++) {
		let a = e.map((e) => [...e]);
		$(a, n, t);
		let o = de(a, t);
		o > i && (i = o, r = n);
	}
	return r;
}
function $(e, t, n) {
	let r = ie[t];
	for (let t = 0; t < n; t++) for (let i = 0; i < n; i++) ue(e, t, i) && r(t, i) && (e[t][i] = e[t][i] ^ 1);
}
function pe(e, t, n) {
	let r = re[t << 2 | n];
	for (let t = 0; t < 8; t++) {
		let n = r >> t & 1;
		e[t + 1][8] = n;
	}
	for (let t = 0; t < 8; t++) {
		let n = r >> 14 - t & 1;
		e[8][t + 1] = n;
	}
}
//#endregion
export { Y as a, U as i, J as n, ae as o, K as r, q as s, H as t };

//# sourceMappingURL=micro-QnXrIYWi.js.map