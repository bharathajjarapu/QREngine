import { a as LinearGradientOptions, c as QRCodeSVGOptions, i as GradientOptions, l as RadialGradientOptions, n as CornerOptions, o as LogoOptions, r as DotType, s as MeasurementUnit, t as BarcodeSVGOptions, u as SVGAccessibilityOptions } from "./_chunks/types.mjs";
import { a as MatrixSVGOptions, i as pdf417, n as datamatrix, o as renderMatrixSVG, r as gs1datamatrix, s as renderMaxiCodeSVG, t as aztec } from "./_chunks/_2d.mjs";
import { n as encodeAztec, t as AztecOptions } from "./_chunks/index.mjs";
import { a as Encode2DResult, c as EncodeType, i as Encode1DResult, n as BarcodeOptions, o as EncodeOptions, r as BarcodeType, s as EncodeResult, t as BarcodeEncodingOptions } from "./_chunks/_types.mjs";
import { A as encodeCode128, C as encodeCode93Extended, D as encodeEAN8, E as encodeEAN13, M as barcodeBase64, N as barcodeDataURI, O as Code128Charset, P as encodeBars, S as encodeCode93, T as encodeCode39Extended, _ as encodeEAN5, a as encodePlessey, b as encodeITF, c as encodeIdentcode, d as encodeCode11, f as encodePharmacode, g as encodeEAN2, h as encodeCodabar, i as encodeGS1DataBarOmni, j as barcode, k as Code128Options, l as encodeLeitcode, m as encodeMSI, n as encodeGS1DataBarExpanded, o as encodePLANET, p as MSICheckDigitType, r as encodeGS1DataBarLimited, s as encodePOSTNET, t as renderBarcodeSVG, u as encodeGS1128, v as encodeUPCA, w as encodeCode39, x as encodeITF14, y as encodeUPCE } from "./_chunks/barcode.mjs";
import { n as encodeGS1DataMatrix, t as encodeDataMatrix } from "./_chunks/index2.mjs";
import { n as ErrorCorrectionLevel, r as QRCodeOptions, t as EncodingMode } from "./_chunks/types2.mjs";
import { a as encodeQR, c as qrcodeDataURI, i as encodeMicroQR, l as qrcodeTerminal, n as renderQRCodeSVG, o as qrcode, r as MicroQROptions, s as qrcodeBase64, t as renderText } from "./_chunks/text.mjs";
import { i as encodePDF417, n as encodeMicroPDF417, r as PDF417Options, t as MicroPDF417Options } from "./_chunks/micropdf417.mjs";
import { _ as qrcodePNG, a as renderMatrixPNG, b as MatrixPNGOptions, c as aztecPNGDataURI, d as datamatrixPNG, f as datamatrixPNGDataURI, g as pdf417PNGDataURI, h as pdf417PNG, i as renderBarcodeRaster, l as barcodePNG, m as gs1datamatrixPNGDataURI, n as RasterData, o as renderMatrixRaster, p as gs1datamatrixPNG, r as renderBarcodePNG, s as aztecPNG, t as encodePNG, u as barcodePNGDataURI, v as qrcodePNGDataURI, y as BarcodePNGOptions } from "./_chunks/png-encoder.mjs";

//#region src/_encode.d.ts
/**
 * Encode data without rendering — returns raw bars or matrix
 */
declare function encode(text: string, options?: EncodeOptions): EncodeResult;
//#endregion
//#region src/_helpers.d.ts
type QROpts = QRCodeSVGOptions & QRCodeOptions;
/** Generate a WiFi QR code */
declare function wifi(ssid: string, password: string, options?: QROpts & {
  encryption?: "WPA" | "WEP" | "nopass";
  hidden?: boolean;
}): string;
/** Generate a URL QR code */
declare function url(urlString: string, options?: QROpts): string;
/** Generate an email QR code */
declare function email(address: string, options?: QROpts): string;
/** Generate an SMS QR code */
declare function sms(number: string, body?: string, options?: QROpts): string;
/** Generate a geo location QR code */
declare function geo(lat: number, lng: number, options?: QROpts): string;
/** Generate a phone call QR code */
declare function phone(number: string, options?: QROpts): string;
/** Generate a vCard QR code */
declare function vcard(contact: {
  firstName: string;
  lastName?: string;
  phone?: string;
  email?: string;
  org?: string;
  title?: string;
  url?: string;
  address?: string;
}, options?: QROpts): string;
/** Generate a MeCard QR code */
declare function mecard(contact: {
  name: string;
  phone?: string;
  email?: string;
  url?: string;
  address?: string;
}, options?: QROpts): string;
/** Generate a calendar event QR code */
declare function event(ev: {
  title: string;
  start: Date | string;
  end?: Date | string;
  location?: string;
  description?: string;
}, options?: QROpts): string;
/** Generate a Swiss QR Code for QR-bill payments */
declare function swissQR(data: {
  iban: string;
  creditor: {
    name: string;
    street?: string;
    houseNumber?: string;
    postalCode: string;
    city: string;
    country: string;
  };
  amount?: number;
  currency?: "CHF" | "EUR";
  debtor?: {
    name: string;
    street?: string;
    houseNumber?: string;
    postalCode: string;
    city: string;
    country: string;
  };
  reference?: string;
  referenceType?: "QRR" | "SCOR" | "NON";
  additionalInfo?: string;
}, options?: QROpts): string;
/** Generate a GS1 Digital Link QR code */
declare function gs1DigitalLink(data: {
  gtin: string;
  batch?: string;
  serial?: string;
  expiry?: string;
  weight?: string;
  lot?: string;
  [key: string]: string | undefined;
}, options?: QROpts & {
  domain?: string;
}): string;
//#endregion
//#region src/encoders/gs1-composite.d.ts
/**
 * GS1 Composite Component encoder
 * Adds supplemental 2D component to a primary 1D barcode
 *
 * CC-A: MicroPDF417-based, small capacity
 * CC-B: MicroPDF417-based, medium capacity
 * CC-C: PDF417-based, large capacity
 *
 * The composite component encodes additional GS1 AI data
 * that doesn't fit in the primary linear barcode.
 */
type CompositeType = "CC-A" | "CC-B" | "CC-C";
interface GS1CompositeResult {
  /** The 2D composite component matrix */
  composite: boolean[][];
  /** Composite type used */
  type: CompositeType;
  /** Number of rows in composite */
  rows: number;
  /** Width in modules */
  cols: number;
}
/**
 * Encode GS1 Composite Component
 * Input: AI string in parenthesized format
 *
 * @param data - GS1 AI data for composite (e.g., "(17)260101(10)BATCH01")
 * @param type - Composite type: CC-A (small), CC-B (medium), CC-C (large)
 * @returns Composite component matrix
 */
declare function encodeGS1Composite(data: string, type?: CompositeType): GS1CompositeResult;
//#endregion
//#region src/encoders/fourstate.d.ts
/**
 * 4-state barcode encoders
 * Shared engine for RM4SCC (Royal Mail), KIX (Dutch), and related postal formats
 *
 * 4-state barcodes use bars with 4 possible states:
 * - Tracker (T): short center bar
 * - Ascender (A): extends above center
 * - Descender (D): extends below center
 * - Full (F): extends both above and below
 */
/** Bar state in a 4-state barcode */
type FourState = "T" | "A" | "D" | "F";
/**
 * Encode Royal Mail 4-State Customer Code (RM4SCC)
 * Used by Royal Mail for automated letter sorting
 *
 * @param text - Postcode + Delivery Point Suffix (alphanumeric, A-Z 0-9)
 * @returns Array of FourState values
 */
declare function encodeRM4SCC(text: string): FourState[];
/**
 * Encode KIX (Klant Index) barcode — Dutch PostNL
 * Same encoding as RM4SCC but without start/stop bars and no check digit
 *
 * @param text - 6 characters (postcode part)
 * @returns Array of FourState values
 */
declare function encodeKIX(text: string): FourState[];
/**
 * Encode Australia Post 4-State barcode
 *
 * @param fcc - Format control code: "11", "59", "62"
 * @param dpid - 8-digit Delivery Point Identifier
 */
declare function encodeAustraliaPost(fcc: string, dpid: string): FourState[];
/**
 * Encode Japan Post 4-State Customer barcode (JP4SCC / Kasutama)
 *
 * @param zipcode - 7-digit Japanese postal code
 * @param address - Optional address characters (digits, dash, A-Z; up to 13 chars)
 */
declare function encodeJapanPost(zipcode: string, address?: string): FourState[];
//#endregion
//#region src/encoders/imb.d.ts
/**
 * Encode USPS Intelligent Mail barcode
 *
 * @param trackingCode - 20-digit tracking code (barcode ID + service type + mailer ID + serial)
 * @param routingCode - ZIP code: empty (0 digits), ZIP5 (5 digits), ZIP+4 (9 digits), or delivery point (11 digits)
 * @returns Array of 65 FourState values
 */
declare function encodeIMb(trackingCode: string, routingCode?: string): FourState[];
//#endregion
//#region src/encoders/codablock-f.d.ts
/**
 * Codablock F encoder — stacked Code 128 barcode
 * Used in healthcare and electronics for compact labeling
 *
 * Structure: multiple rows of Code 128 with row indicators
 * Each row: Start C + row indicator + data codewords + check + Stop
 */
interface CodablockFResult {
  matrix: boolean[][];
  rows: number;
  cols: number;
}
/**
 * Encode text as Codablock F (stacked Code 128)
 *
 * @param text - Text to encode
 * @param options - columns: data columns per row (default 4-10 auto)
 */
declare function encodeCodablockF(text: string, options?: {
  columns?: number;
}): CodablockFResult;
//#endregion
//#region src/encoders/code16k.d.ts
/**
 * Code 16K encoder — stacked barcode based on Code 128
 * Used in healthcare and electronics
 *
 * Structure: 2-16 rows, each with start pattern + 5 symbol characters + stop pattern
 * Uses Code 128 bar patterns with row-specific start codes
 */
interface Code16KResult {
  matrix: boolean[][];
  rows: number;
  cols: number;
}
/**
 * Encode text as Code 16K
 *
 * @param text - Printable ASCII text
 * @returns Stacked barcode matrix
 */
declare function encodeCode16K(text: string): Code16KResult;
//#endregion
//#region src/encoders/maxicode.d.ts
/**
 * MaxiCode encoder (ISO/IEC 16023)
 * Fixed-size 2D barcode used on UPS shipping labels
 *
 * Structure:
 * - 33 rows x 30 columns hexagonal grid (884 modules, 864 data bits)
 * - Central bullseye finder pattern
 * - 6 encoding modes (2/3 for structured carrier, 4/5/6 for general)
 * - Reed-Solomon error correction over GF(64)
 */
interface MaxiCodeOptions {
  /** Encoding mode: 2 (US structured), 3 (intl structured), 4 (standard), 5 (full ECC), 6 (reader programming) */
  mode?: 2 | 3 | 4 | 5 | 6;
  /** Postal code (modes 2/3) */
  postalCode?: string;
  /** ISO country code number (modes 2/3) */
  countryCode?: number;
  /** Service class (modes 2/3, e.g. 840 for UPS) */
  serviceClass?: number;
}
/**
 * Encode text as MaxiCode
 * Returns a 33x30 boolean matrix (hexagonal grid representation)
 */
declare function encodeMaxiCode(text: string, options?: MaxiCodeOptions): boolean[][];
//#endregion
//#region src/encoders/rmqr.d.ts
/**
 * rMQR (Rectangular Micro QR Code) encoder — ISO/IEC 23941
 * Rectangular QR code for constrained spaces (medical tubes, PCB, tickets)
 *
 * Features:
 * - 32 symbol sizes from R7x43 to R17x139
 * - Single finder pattern (top-left) + alignment patterns
 * - EC levels M and H only
 * - Numeric, alphanumeric, byte, kanji modes
 */
interface RMQROptions {
  ecLevel?: "M" | "H";
  version?: number;
}
/**
 * Encode text as rMQR (Rectangular Micro QR Code)
 * Returns a rectangular boolean matrix
 */
declare function encodeRMQR(text: string, options?: RMQROptions): boolean[][];
//#endregion
//#region src/encoders/dotcode.d.ts
/**
 * DotCode encoder (AIM ISS DotCode 4.0)
 * High-speed industrial 2D barcode for tobacco, pharma
 *
 * Structure:
 * - Rectangular grid of dots (not connected bars)
 * - Checkerboard-like pattern — only even (r+c) positions filled
 * - Variable size, height + width must be odd
 * - GF(113) Reed-Solomon error correction (prime field, alpha = 3)
 */
/**
 * Encode text as DotCode.
 * Returns a 2D boolean matrix (true = dot present).
 */
declare function encodeDotCode(text: string): boolean[][];
//#endregion
//#region src/encoders/hanxin.d.ts
/**
 * Han Xin Code encoder (ISO/IEC 20830)
 * Chinese 2D barcode standard — large capacity for Chinese characters
 *
 * Features:
 * - 84 versions (23×23 to 189×189 modules)
 * - 4 EC levels (L1-L4: ~8%, ~15%, ~23%, ~30%)
 * - Modes: numeric, text, binary, Chinese (GB 18030)
 * - Finder patterns at all 4 corners (unlike QR's 3)
 */
interface HanXinOptions {
  ecLevel?: 1 | 2 | 3 | 4;
  version?: number;
}
/**
 * Encode text as Han Xin Code
 * Returns a 2D boolean matrix
 */
declare function encodeHanXin(text: string, options?: HanXinOptions): boolean[][];
//#endregion
//#region src/encoders/jabcode.d.ts
/**
 * JAB Code encoder (ISO/IEC 23634)
 * Polychrome (colored) 2D barcode — new ISO standard
 *
 * Features:
 * - Uses 4 or 8 colors instead of black/white (2-3x capacity vs QR)
 * - Square matrix with finder patterns
 * - LDPC-like XOR-parity error correction
 * - Version 1-32
 *
 * Note: JAB Code output is a color matrix, not boolean.
 * Each cell has a color index (0-3 for 4-color, 0-7 for 8-color).
 */
/** JAB Code color palette — 4-color default */
declare const JAB_COLORS_4: readonly ["#000000", "#FF0000", "#00FF00", "#0000FF"];
/** 8-color palette */
declare const JAB_COLORS_8: readonly ["#000000", "#FF0000", "#00FF00", "#0000FF", "#FFFF00", "#FF00FF", "#00FFFF", "#FFFFFF"];
interface JABCodeOptions {
  /** Number of colors: 4 or 8 (default 4) */
  colors?: 4 | 8;
  /** EC percentage (default 20) */
  ecPercent?: number;
}
interface JABCodeResult {
  /** 2D matrix of color indices (0 to colors-1) */
  matrix: number[][];
  /** Number of rows */
  rows: number;
  /** Number of columns */
  cols: number;
  /** Color palette */
  palette: readonly string[];
}
/**
 * Encode text as JAB Code
 * Returns a color index matrix (not boolean — each cell is a color index)
 */
declare function encodeJABCode(text: string, options?: JABCodeOptions): JABCodeResult;
//#endregion
//#region src/encoders/hibc.d.ts
/**
 * HIBC (Health Industry Bar Code) encoder
 * Data formatting layer for medical device labeling (ANSI/HIBC 2.6)
 *
 * HIBC Primary: +LIC ProductNumber UnitOfMeasure CheckDigit
 * HIBC Secondary: +$$ ExpiryDate LotNumber CheckDigit
 */
/**
 * Encode HIBC Primary data (LIC + product info)
 * Returns the formatted HIBC string ready for barcode encoding
 *
 * @param lic - Labeler Identification Code (4 chars, uppercase alphanumeric)
 * @param product - Product/Catalog number (1-18 chars)
 * @param unitOfMeasure - Unit of measure digit (0-9, default 0)
 */
declare function encodeHIBCPrimary(lic: string, product: string, unitOfMeasure?: number): string;
/**
 * Encode HIBC Secondary data (expiry + lot/batch)
 * Returns the formatted HIBC string ready for barcode encoding
 *
 * @param expiry - Expiry date (YYYYMMDD or YYMMDD or YYMM)
 * @param lot - Lot/batch number (up to 18 chars)
 */
declare function encodeHIBCSecondary(expiry?: string, lot?: string): string;
/**
 * Encode HIBC concatenated (primary + secondary in one barcode)
 */
declare function encodeHIBCConcatenated(lic: string, product: string, options?: {
  unitOfMeasure?: number;
  expiry?: string;
  lot?: string;
}): string;
//#endregion
//#region src/encoders/isbt128.d.ts
/**
 * ISBT 128 encoder for blood bank labeling
 * International Society of Blood Transfusion standard
 *
 * ISBT 128 uses Code 128 as the symbology with specific data structures
 * for donation identification, blood components, expiry dates, etc.
 */
/**
 * Compute ISO/IEC 7064 Modulo 37-2 check character
 *
 * Algorithm (pure system, radix 2, modulus 37):
 * 1. Start with sum = 0
 * 2. For each character: sum = ((sum + charValue) * 2) mod 37
 * 3. Check value = (37 - sum) mod 37
 * 4. Map back to character set
 *
 * @param data - Input string containing only characters from the Mod 37-2 character set
 * @returns Single check character
 */
declare function iso7064Mod37_2(data: string): string;
/**
 * Encode ISBT 128 Donation Identification Number (DIN)
 * Format: =FLLLLLYYYNNNNNN where:
 *   = : ISBT flag character
 *   F : Facility ID (2 chars country code from ICCBBA)
 *   LLLLL : Facility number (5 chars)
 *   YYY : Year + day-of-year or sequential
 *   NNNNNN : Donation number (6 chars)
 *
 * @param countryCode - 2-char country facility code
 * @param facilityNumber - 5-char facility number
 * @param year - 2-digit year
 * @param donationNumber - Up to 6 chars donation sequence
 * @returns ISBT 128 formatted string for Code 128 encoding
 */
declare function encodeISBT128DIN(countryCode: string, facilityNumber: string, year: string, donationNumber: string): string;
/**
 * Encode ISBT 128 Blood Component code
 * 5-character product code from ICCBBA product description codes
 *
 * @param productCode - 5-char ICCBBA product code (e.g., "E0791" for red blood cells)
 * @returns ISBT 128 formatted string
 */
declare function encodeISBT128Component(productCode: string): string;
/**
 * Encode ISBT 128 Expiry Date
 * Format: &YYMMDD (& prefix for expiry)
 *
 * @param date - Date string in YYMMDD format
 * @returns ISBT 128 formatted expiry string
 */
declare function encodeISBT128Expiry(date: string): string;
/**
 * Encode ISBT 128 Blood Group
 * Format: %BG where BG is the ABO/Rh code
 *
 * @param bloodGroup - Blood group code (e.g., "51" for O Pos, "55" for A Pos)
 * @returns ISBT 128 formatted blood group string
 */
declare function encodeISBT128BloodGroup(bloodGroup: string): string;
//#endregion
//#region src/renderers/data-uri.d.ts
/**
 * Data URI encoding utilities
 */
/**
 * Convert SVG string to a data URI
 */
declare function svgToDataURI(svg: string): string;
/**
 * Convert SVG string to base64 data URI
 */
declare function svgToBase64(svg: string): string;
/**
 * Convert SVG string to a plain base64 string (no data URI prefix)
 */
declare function svgToBase64Raw(svg: string): string;
//#endregion
//#region src/renderers/svg/optimize.d.ts
/**
 * SVG output optimization utilities
 */
/**
 * Optimize SVG string for smaller output size
 * - Rounds decimal numbers to specified precision
 * - Removes trailing zeros (e.g., "4.00" → "4")
 * - Optionally removes width/height for responsive SVGs (viewBox only)
 */
declare function optimizeSVG(svg: string, options?: {
  /** Decimal precision for numbers (default 2) */precision?: number; /** Remove width/height attributes for fully responsive SVG */
  responsive?: boolean;
}): string;
//#endregion
//#region src/errors.d.ts
/**
 * Custom error classes for etiket
 */
declare class EtiketError extends Error {
  name: string;
}
declare class InvalidInputError extends EtiketError {
  name: string;
}
declare class CapacityError extends EtiketError {
  name: string;
}
declare class CheckDigitError extends EtiketError {
  name: string;
}
//#endregion
//#region src/validators/barcode.d.ts
/**
 * Barcode input validation utilities
 */
/** Validate and calculate EAN/UPC check digit (modulo 10 weighted) */
declare function calculateEANCheckDigit(digits: number[]): number;
/** Verify EAN/UPC check digit */
declare function verifyEANCheckDigit(text: string): boolean;
/** Validate barcode input for a given type */
declare function validateBarcode(text: string, type: string): {
  valid: boolean;
  error?: string;
};
/** Check if input is valid for a barcode type (boolean convenience) */
declare function isValidInput(text: string, type: string): boolean;
/**
 * Validate barcode input and return metadata including check digit where applicable.
 *
 * Reuses `validateBarcode` for basic validation, then computes the check digit
 * for barcode types that support it (EAN-13, EAN-8, UPC-A, ITF-14).
 */
declare function validateBarcodeInput(text: string, type: string): {
  valid: boolean;
  error?: string;
  checkDigit?: number;
};
//#endregion
//#region src/validators/qr.d.ts
interface QRValidationResult {
  valid: boolean;
  error?: string;
  /** Minimum QR version needed (1-40), only when valid */
  version?: number;
  /** Detected encoding mode */
  mode?: "numeric" | "alphanumeric" | "byte";
  /** Data length in the detected mode's units */
  dataLength?: number;
  /** Maximum capacity for the detected mode and EC level */
  maxCapacity?: number;
}
/** Validate QR code input */
declare function validateQRInput(text: string, ecLevel?: ErrorCorrectionLevel): QRValidationResult;
//#endregion
export { type AztecOptions, type BarcodeEncodingOptions, type BarcodeOptions, type BarcodePNGOptions, type BarcodeSVGOptions, type BarcodeType, CapacityError, CheckDigitError, type Code128Charset, type Code128Options, type CompositeType, type CornerOptions, type DotType, type Encode1DResult, type Encode2DResult, type EncodeOptions, type EncodeResult, type EncodeType, type EncodingMode, type ErrorCorrectionLevel, EtiketError, type FourState, type GS1CompositeResult, type GradientOptions, type HanXinOptions, InvalidInputError, type JABCodeOptions, type JABCodeResult, JAB_COLORS_4, JAB_COLORS_8, type LinearGradientOptions, type LogoOptions, type MSICheckDigitType, type MatrixPNGOptions, type MatrixSVGOptions, type MaxiCodeOptions, type MeasurementUnit, type MicroPDF417Options, type MicroQROptions, type PDF417Options, type QRCodeOptions, type QRCodeSVGOptions, type QRValidationResult, type RMQROptions, type RadialGradientOptions, type RasterData, type SVGAccessibilityOptions, aztec, aztecPNG, aztecPNGDataURI, barcode, barcodeBase64, barcodeDataURI, barcodePNG, barcodePNGDataURI, calculateEANCheckDigit, datamatrix, datamatrixPNG, datamatrixPNGDataURI, email, encode, encodeAustraliaPost, encodeAztec, encodeBars, encodeCodabar, encodeCodablockF, encodeCode11, encodeCode128, encodeCode16K, encodeCode39, encodeCode39Extended, encodeCode93, encodeCode93Extended, encodeDataMatrix, encodeDotCode, encodeEAN13, encodeEAN2, encodeEAN5, encodeEAN8, encodeGS1128, encodeGS1Composite, encodeGS1DataBarExpanded, encodeGS1DataBarLimited, encodeGS1DataBarOmni, encodeGS1DataMatrix, encodeHIBCConcatenated, encodeHIBCPrimary, encodeHIBCSecondary, encodeHanXin, encodeIMb, encodeISBT128BloodGroup, encodeISBT128Component, encodeISBT128DIN, encodeISBT128Expiry, encodeITF, encodeITF14, encodeIdentcode, encodeJABCode, encodeJapanPost, encodeKIX, encodeLeitcode, encodeMSI, encodeMaxiCode, encodeMicroPDF417, encodeMicroQR, encodePDF417, encodePLANET, encodePNG, encodePOSTNET, encodePharmacode, encodePlessey, encodeQR, encodeRM4SCC, encodeRMQR, encodeUPCA, encodeUPCE, event, geo, gs1DigitalLink, gs1datamatrix, gs1datamatrixPNG, gs1datamatrixPNGDataURI, isValidInput, iso7064Mod37_2, mecard, optimizeSVG, pdf417, pdf417PNG, pdf417PNGDataURI, phone, qrcode, qrcodeBase64, qrcodeDataURI, qrcodePNG, qrcodePNGDataURI, qrcodeTerminal, renderBarcodePNG, renderBarcodeRaster, renderBarcodeSVG, renderMatrixPNG, renderMatrixRaster, renderMatrixSVG, renderMaxiCodeSVG, renderQRCodeSVG, renderText, sms, svgToBase64, svgToBase64Raw, svgToDataURI, swissQR, url, validateBarcode, validateBarcodeInput, validateQRInput, vcard, verifyEANCheckDigit, wifi };