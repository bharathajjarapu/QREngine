//#region src/encoders/pdf417/index.d.ts
/**
 * PDF417 barcode encoder — ISO/IEC 15438 implementation
 * Stacked 2D barcode with rows of codewords (each 17 modules wide)
 *
 * Supports text, byte, and numeric compaction modes with
 * Reed-Solomon error correction over GF(929).
 */
interface PDF417Options {
  /** Error correction level 0-8, default auto-selected based on data size */
  ecLevel?: number;
  /** Number of data columns (1-30), auto-calculated if omitted */
  columns?: number;
  /** Compact PDF417 (omits right row indicator) */
  compact?: boolean;
}
interface PDF417Result {
  /** 2D boolean matrix (true = black bar, false = white space) */
  matrix: boolean[][];
  /** Number of rows in the symbol */
  rows: number;
  /** Number of module columns in the symbol */
  cols: number;
}
/**
 * Encode text as a PDF417 barcode.
 *
 * @param text - The text to encode
 * @param options - Optional encoding parameters
 * @returns Object with boolean matrix and dimensions
 *
 * @example
 * ```ts
 * const result = encodePDF417('Hello, World!')
 * // result.matrix is a 2D boolean array
 * // result.rows and result.cols give the dimensions
 * ```
 */
declare function encodePDF417(text: string, options?: PDF417Options): PDF417Result;
//#endregion
//#region src/encoders/micropdf417.d.ts
/**
 * MicroPDF417 encoder (ISO/IEC 24728)
 * Compact variant of PDF417 for small items
 *
 * Features:
 * - 1-4 data columns (vs 1-30 in PDF417)
 * - 4-44 rows
 * - Uses Row Address Patterns (RAPs) instead of PDF417 start/stop
 * - Smaller than standard PDF417 for short data
 */
interface MicroPDF417Options {
  columns?: 1 | 2 | 3 | 4;
}
interface MicroPDF417Result {
  matrix: boolean[][];
  rows: number;
  cols: number;
}
/**
 * Encode text as MicroPDF417
 */
declare function encodeMicroPDF417(text: string, options?: MicroPDF417Options): MicroPDF417Result;
//#endregion
export { encodePDF417 as i, encodeMicroPDF417 as n, PDF417Options as r, MicroPDF417Options as t };