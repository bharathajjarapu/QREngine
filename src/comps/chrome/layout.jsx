/** Page shell: full-height grain background. */
export function Page({ children }) {
  return <main className="page-grain flex min-h-[100dvh] w-full max-w-full flex-col overflow-x-hidden font-sans text-qn-ink">{children}</main>
}
